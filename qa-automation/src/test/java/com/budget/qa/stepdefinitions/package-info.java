package com.budget.qa.stepdefinitions;

/**
 * Cucumber Step Definitions package for Budget Management App.
 *
 * <p>Step definitions map Gherkin steps to Screenplay Tasks, Actions, and Questions.
 * Each step definition class should correspond to a feature domain:</p>
 *
 * <ul>
 *     <li>{@code AuthStepDefinitions} — login, registration, token management</li>
 *     <li>{@code TransactionStepDefinitions} — CRUD operations on transactions</li>
 *     <li>{@code ReportStepDefinitions} — report queries, recalculation, PDF download</li>
 * </ul>
 *
 * <p><strong>Pattern:</strong> Step definitions should be thin wrappers that delegate
 * to Screenplay Tasks. Business logic belongs in Tasks, not here.</p>
 *
 * @see com.budget.qa.tasks
 * @see com.budget.qa.questions
 */
// Package-level documentation. Step definition classes will be added in Sprint 1.
